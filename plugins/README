Copy the plugin file you want to ~/.cpanm/plugins

TODO:
- fix cp562an to actually query the distribution (parse 02packages?)
- gitpan/backpan plugin to get a specific version from from BackPAN or GitPAN
